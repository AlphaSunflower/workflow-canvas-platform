# Canvas Performance Recorder Instrumentation Map

| ID | File path | Function/component | Event type | Recorded fields | Purpose | Sampling | Temporary | Cleanup condition | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CPR-001 | `src/components/canvas/Canvas.tsx` | viewport callbacks | `operation.pan`, `operation.zoom` | viewport, importing, node counts | Correlate pan/zoom with stalls | start/end plus sampled counters | yes | Remove after canvas performance baseline is stable | Uses adapter only |
| CPR-002 | `src/components/canvas/Canvas.tsx` | import lifecycle | `operation.import` | batch size, completed, failed, duration | Correlate import with long tasks | start/end/progress summary | yes | Remove after import performance regression suite is stable | No file names |
| CPR-003 | `src/utils/performance/canvas-image-performance.ts` | `recordCanvasRenderPlanMetric` bridge, called from `src/components/canvas/Canvas.tsx` | `renderPlan.build` | node counts, mode counts, duration | Detect render-plan stalls | per render plan metric | yes | Remove if existing summary is sufficient | Mirrors diagnostics |
| CPR-004 | `src/components/canvas/useCanvasBoxSelection.ts` | selection lifecycle | `operation.boxSelect` | phase only | Detect selection-triggered flicker/stalls | start/end only | yes | Remove after selection regression is stable | No selection geometry |
| CPR-005 | `src/components/canvas/CanvasImageRasterLayer.tsx` | draw callback | `raster.draw`, `raster.pixiFallback`, `raster.backendFallback`, `raster.textureUpload`, `raster.textureEvict`, `raster.spritePool` | backend, item counts, duration, texture and sprite counts | Detect slow raster draws, Pixi fallback, GPU upload bursts, and sprite churn | per draw/fallback/upload/evict/pool change | yes | Remove after raster backend is stable | Uses adapter only |
| CPR-006 | `src/components/canvas/canvas-image-resource-controller.ts` | resource batch | `resource.batch` | counts, duration | Detect request bursts | per batch | yes | Remove after resource controller is stable | Uses adapter only |
| CPR-007 | `src/components/canvas/canvas-raster-ready-store.ts` | ready store commit | `raster.readyStoreCommit` | version, item count | Detect ready snapshot redraw pressure | per changed commit | yes | Remove after ready store behavior is stable | Uses adapter only |
| CPR-008 | `src/components/canvas/canvas-node-patch-queue.ts` | queue operations | `nodePatch.enqueue`, `nodePatch.flush`, `nodePatch.cancel` | counts, duration | Detect patch queue pressure | per queue operation | yes | Remove after import patch queue baseline is stable | Uses adapter only |
| CPR-009 | `src/services/image/image-manager.ts` | resource lifecycle | `imageManager.emit`, `imageManager.request`, `imageManager.cancel` | mode, status | Detect image state broadcast spikes | emit counted per second | yes | Remove after image manager emit/sec stays bounded | Node ids hashed |
| CPR-010 | `src/services/file/image-thumbnail-worker-pipeline.ts` | worker queue diagnostics | `thumbnail.queue`, `thumbnail.start`, `thumbnail.done`, `thumbnail.error` | queue counts, duration | Detect thumbnail queue stalls | per queue event | yes | Remove after thumbnail pipeline baseline is stable | Task ids hashed |
| CPR-011 | `tools/canvas-performance-recorder/*` | recorder internals | `longtask`, `frame.gap`, `memory.sample`, `summary.sample` | durations, heap sizes | Correlate main thread pressure with events | long tasks/frame gaps + sampled memory | yes | Keep while recorder exists | Browser support dependent |
